- DONE proper restart (right now restart does not do a proper reset and story variables evaluate to NaN)
- DONE save and load, offer a save/load dialog with several save slots, group by playthrough, metadata on saves (game id, etc, timestamp), delete/rename, export/import to/from disk
- DONE storing saves in an indexedDB 
- check sugarcube macros and decide which to implement equivalently (some are not needed, e.g. <<do>>, <<redo>> etc)
- implement macros from previous step
- DONE check handling of uninitialised variables. One common culprit of sugarcube was that authors queried a variable before setting it, resulting in undefined returns. Is there a better way of handling this? We shouldn't get NaN or undefineds in our output
- DONE setup prettier with strict defaults in this project
- fix all warnings 
- add github actions for testing merge requests and building and shipping releases
- passage tracking system (visited and rendered)